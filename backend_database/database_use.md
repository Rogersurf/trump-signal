To set up the database for the project, follow these steps:
 1. run init_db.py to create the database and tables:
    ```bash
    python init_db.py
    ```
2. run daily_update.py to populate the database with daily data:
    ```bash
    python daily_update.py
    ```